# Whetstone-envs minigrid environment — spec-out placeholder

**Kind:** staging (placeholder by decision, 2026-08-24). Danielle asked for the
placeholder now and the spec-out later — do not expand this document until she
initiates the spec.

**Provenance.** From the 2026-08-23 infrastructure conversation (recorded in
`../../potential-projs/text-latent-code-autoencoder.md` §4, current-state entry):
the `whetstone-envs` repository contains a minigrid environment that Danielle
flagged as a candidate to spec into a small standalone project. Prior-art intake
waiting for the spec-out: the LLM-as-optimizer / toy-RL-with-minimal-translation
survey in `../reference/prompt-optimization-landscape.md` (undated Perplexity
task, intaken 2026-08-24 — CartPole/minigrid named in her prompt; sensory-motor
control via iterative prompting, Bosio & Mueller, Atari-GPT negative result,
outer-loop-vs-inner-loop conclusion).

**What the spec-out would cover (when initiated):** what the env currently
implements, what a small project around it would measure or demonstrate, how it
relates to the verified dr-*/whetstone optimizer stack, and whether it earns a
project prefix or gets absorbed.

**Gate.** Danielle decides whether and when to spec it. Exits: a real staging spec
(then the normal promotion path), or deletion if the env stays
infrastructure-only.
